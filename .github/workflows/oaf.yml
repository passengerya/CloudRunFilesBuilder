#=================================================
# https://github.com/wukongdaily/RunFilesBuilder
# Description: Build RunFiles using GitHub Actions
# Lisence: MIT
# Author: wukongdaily
# Blog: wkdaily.cpolar.cn
#=================================================

# 应用过滤 OpenAppFilter (destan19)
name: Build 24.10 OpenAppFilter

on:
  schedule:
    - cron: "10 22 * * *"  # 北京时间早上 6 点 10
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Clone makeself repository
        run: git clone https://github.com/megastep/makeself.git

      - name: Fetch latest release info
        run: |
          curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" https://api.github.com/repos/destan19/OpenAppFilter/releases?per_page=20 \
            | jq '[.[] | select(.prerelease==false) | select(any(.assets[]; .name | test("^luci-app-oaf_.*_all\\.ipk$")))] | sort_by(.published_at) | last' \
            > release.json

      - name: Parse asset download URLs
        run: |
          MAIN_URL=$(jq -r '.assets[] | select(.name | test("^luci-app-oaf_.*_all\\.ipk$")) | .browser_download_url' release.json)
          I18N_URL=$(jq -r '.assets[] | select(.name | test("^luci-i18n-oaf-zh-cn_.*_all\\.ipk$")) | .browser_download_url' release.json)
          echo "MAIN_URL=$MAIN_URL" >> $GITHUB_ENV
          echo "I18N_URL=$I18N_URL" >> $GITHUB_ENV

          # 从 ipk 文件名提取版本号 如 luci-app-oaf_1.2.0-r20260808_all.ipk -> 1.2.0-r20260808
          VERSION=$(echo "$MAIN_URL" | sed -n 's/.*luci-app-oaf_\([0-9][^_]*\)_all\.ipk.*/\1/p')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "提取到版本号: $VERSION"

      - name: Download ipk files
        run: |
          mkdir -p oaf
          wget "$MAIN_URL" -P oaf/ 2>/dev/null || echo "⚠️ 无主 ipk 资产"
          wget "$I18N_URL" -P oaf/ 2>/dev/null || echo "⚠️ 无 zh-cn i18n 资产"
          ls -lh oaf/

      - name: Fix oaf uci-defaults heredoc indentation
        # 上游 94_feature_3.0 的 heredoc 体用空格缩进, <<-EOF 只剥 tab,
        # 导致 uci batch 收到带前导空格的命令静默失败 -> /etc/config/appfilter
        # 首启不生成、oaf 菜单不显示(2026-09-23 下游固件实测)。
        # 这里把行首空白统一转为 tab(幂等: 上游修复后本步为无害空操作;
        # 95_time_daily_limit 本就是 tab 缩进, 不受影响)。
        run: |
          fix_oaf_ipk() {
              ipk="$1"
              tmp=$(mktemp -d)
              if ! tar -xzf "$ipk" -C "$tmp" 2>/dev/null; then
                  echo "⚠️ 解包失败, 跳过: $(basename "$ipk")"
                  rm -rf "$tmp"
                  return
              fi
              if [ ! -f "$tmp/data.tar.gz" ] || [ ! -f "$tmp/control.tar.gz" ]; then
                  echo "⚠️ ipk 布局不兼容, 跳过: $(basename "$ipk")"
                  rm -rf "$tmp"
                  return
              fi
              mkdir -p "$tmp/data"
              tar -xzf "$tmp/data.tar.gz" -C "$tmp/data"
              sed -i 's/^[[:space:]]\+\(set \|commit \)/\t\1/' \
                  "$tmp/data/etc/uci-defaults/94_feature_3.0" 2>/dev/null || true
              tar -czf "$tmp/data.tar.gz" -C "$tmp/data" .
              tar -czf "$ipk.new" -C "$tmp" debian-binary data.tar.gz control.tar.gz
              mv "$ipk.new" "$ipk"
              rm -rf "$tmp"
              echo "✅ 已修复 heredoc 缩进: $(basename "$ipk")"
          }
          for ipk in oaf/luci-app-oaf_*.ipk; do
              if [ -f "$ipk" ]; then
                  fix_oaf_ipk "$ipk"
              fi
          done
          echo "--- 修复后 94_feature_3.0 前 4 行(应显示 \\t 即 tab) ---"
          tar -xzOf oaf/luci-app-oaf_*.ipk data.tar.gz 2>/dev/null \
            | tar -xzO ./etc/uci-defaults/94_feature_3.0 2>/dev/null | sed -n '2,5l' || true

      - name: Create install.sh script
        run: |
          cat <<EOF > oaf/install.sh
          #!/bin/sh
          opkg update
          if [ \$? -ne 0 ]; then
              echo "update failed。"
              exit 1
          fi
          opkg install *.ipk
          EOF
          chmod +x oaf/install.sh

      - name: Move directory to makeself
        run: mv oaf makeself/

      - name: Create self-extracting package
        run: |
          cd makeself
          ./makeself.sh oaf/ luci-app-oaf-${VERSION}_all.run "by github action" ./install.sh

      - name: Check file sizes
        run: ls -lh makeself/*.run

      - name: Set tag name
        id: vars
        run: |
          TODAY=$(date -u -d '+8 hour' +'%Y-%m-%d')
          echo "tag_name=$TODAY" >> $GITHUB_ENV

      - name: Upload run files as release assets
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.tag_name }}
          name: "Daily Build - ${{ env.tag_name }}"
          files: makeself/*.run
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
             <p align="left">
              <img src="https://img.shields.io/github/downloads/passengerya/CloudRunFilesBuilder/${{ env.tag_name }}/total?style=for-the-badge&label=%E4%BB%8A%E6%97%A5%E4%B8%8B%E8%BD%BD%E6%AC%A1%E6%95%B0&labelColor=blue&color=black"/>
             </p>
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify Sync Store (builder 全部构建完成后即时触发同步)
        if: success()
        env:
          SYNC_DISPATCH_TOKEN: ${{ secrets.SYNC_DISPATCH_TOKEN }}
        run: |
          if [ -z "$SYNC_DISPATCH_TOKEN" ]; then
            echo "⏭️ 未配置 SYNC_DISPATCH_TOKEN, 跳过通知(依赖 23:00 UTC 定时同步)"
            exit 0
          fi
          # 领导选举: 等 15s 让同期启动的构建全部入队, 然后由「最新启动且仍在运行/排队」
          # 的运行发通知——确定性单通知, 不依赖其它构建的完成状态(轮询方式会死锁:
          # 各运行的等待本身让对方永远看到"运行中")。更晚启动的构建完成后会自行通知。
          sleep 15
          my_ts=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/passengerya/CloudRunFilesBuilder/actions/runs?per_page=100" \
            | jq -r --argjson rid "$GITHUB_RUN_ID" '.workflow_runs[] | select(.id == $rid) | .created_at')
          newer=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/passengerya/CloudRunFilesBuilder/actions/runs?per_page=100" \
            | jq --argjson rid "$GITHUB_RUN_ID" --arg ts "$my_ts" \
            '[.workflow_runs[] | select(.id != $rid and ((.created_at > $ts) or (.created_at == $ts and .id > $rid)) and (.status == "in_progress" or .status == "queued"))] | length')
          if [ "$newer" != "0" ]; then
            echo "⏳ 有 $newer 个更晚启动的构建运行中, 由它们完成后通知 Sync Store"
            exit 0
          fi
          code=$(curl -s -o /dev/null -w '%{http_code}' -X POST \
            -H "Authorization: token $SYNC_DISPATCH_TOKEN" -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/passengerya/AutoBuildTWrt/dispatches" \
            -d '{"event_type":"builder-done"}')
          echo "✅ 已通知 Sync Store (HTTP $code)"
